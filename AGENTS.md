# WePChat Flutter 工程约束协议

这份文件是项目的工程约束，适用于所有参与代码编写、重构、修复和审查的 Agent。

目标不是堆叠规则，而是让代码保持小、清晰、可复用、可维护。遇到具体任务时，优先完成任务本身，不顺手扩大改动范围。

## 1. 总体原则

### 1.1 先理解，再修改

- 修改前先阅读相关模块、调用方和已有测试。
- 先搜索现有实现、工具函数、数据结构和依赖，再决定是否新增代码。
- 不根据文件名或猜测直接重写模块。
- 不为了“顺便整理”而修改与当前任务无关的代码。

### 1.2 复用优先

- 一个行为只能有一个权威实现。
- 新增功能前必须搜索是否已有等价或相近实现。
- 业务层、平台层和工具层不得各自复制一份相同逻辑。
- 路径校验、权限判断、错误格式化、截断、序列化等公共行为必须集中实现。
- 如果两个实现只有少量参数差异，优先抽取清晰的共享函数；不要复制后再逐渐漂移。
- 不为了消除两行重复代码而制造复杂抽象。

### 1.3 失败要明确

- 拒绝无意义的兜底代码和“看起来安全”的静默降级。
- 不使用空 `catch`、`catch (e) { return null; }`、吞掉异常或伪造成功结果。
- 不用 `dynamic`、`Object?`、空字符串、零值或空列表掩盖未知状态。
- 错误应在最接近失败的位置被转换为有意义的领域错误，并继续向上层传播。
- 只有在产品协议明确规定了 fallback 时才实现 fallback，并写清触发条件。
- 如果输入不符合协议，直接返回可诊断的错误；不要猜测用户意图或自动修正到另一个语义。

必要的边界校验不属于被拒绝的防御性代码。路径越界、权限、文件大小、超时、取消、外部 API 响应和用户输入仍必须在边界处校验一次。

## 2. 文件规模与模块拆分

### 2.1 行数上限

- 手写源文件控制在 800 行以内，目标是 300～600 行。
- 任何新文件不得以接近 800 行为目标。
- 文件超过 600 行时，应主动检查是否包含多个职责；超过 800 行必须拆分后再合并。
- 测试文件同样遵守 800 行上限；大测试应按功能域拆分。
- 生成代码、第三方源码和锁文件可豁免，但不得手工修改生成结果来规避限制。

### 2.2 拆分原则

按职责拆分，而不是机械地按函数数量拆分。推荐边界：

```text
UI
  页面、组件、渲染状态

应用层
  用例、会话编排、设置和权限

领域层
  消息、工具、工作区、记忆、Artifact

基础设施层
  HTTP、持久化、QuickJS、平台适配
```

- 一个文件应有一个清晰的主要职责。
- 不把平台分支、网络请求、数据模型和 UI 状态混在一个文件里。
- 不通过大量私有辅助函数把一个本应拆分的模块伪装成单文件。
- 拆分后保持公开接口稳定，避免为了拆分而引入额外层级。

## 3. 代码复杂度

- 优先使用早返回，避免深层嵌套。
- 一个函数应完成一个可描述的动作；复杂流程拆成有命名的步骤。
- 不使用超过实际需要的泛型、抽象基类、状态模式或事件总线。
- 不为未来尚未确定的需求预留多个扩展点。
- 条件分支超过三层，先检查是否混合了多个职责。
- 重复的条件判断应提取为有业务含义的谓词，而不是复制布尔表达式。
- 魔法数字、字符串和超时必须集中定义并命名。
- 注释解释“为什么”，不重复描述代码“做了什么”。
- 公共 API、跨平台约束和非显而易见的安全边界必须有简短文档。

## 4. 类型与数据流

- 优先使用明确的 Dart 类型、sealed class、enum 和不可变数据对象。
- `dynamic` 只允许出现在外部 JSON、JS 桥接或确实无法静态描述的边界，并立即转换为领域类型。
- JSON 解码后不得让未校验的 Map 在业务层到处流动。
- 工具参数在工具入口统一校验；内部函数接收已校验的类型，不重复校验同一条件。
- 工具结果区分成功、领域错误、取消和权限拒绝，不用一段字符串混合表达所有状态。
- 不通过可空字段表达多个互斥状态；必要时使用显式状态类型。

## 5. 错误、日志与取消

### 5.1 错误处理

- 外部错误必须带有来源和上下文，例如工具名、文件路径或请求阶段。
- 不向用户暴露 API Key、完整请求头、内部路径或敏感响应。
- 不在底层重复弹窗；底层返回错误，上层决定展示方式。
- 不把异常转换成“操作成功”或空结果。

### 5.2 日志

- 日志用于诊断真实问题，不用于替代错误处理。
- 不记录凭据、完整文件内容和隐私数据。
- 高噪声循环不得无条件输出 debug 日志。
- 工具执行日志至少能够关联会话、工具名、调用 ID 和结果状态。

### 5.3 异步与取消

- 所有网络、文件、JS 执行和图片请求都必须支持取消。
- 页面销毁或会话停止后，不得继续更新已失效的 UI 状态。
- 不在 Flutter UI isolate 中执行可能阻塞的 JS、压缩、解析或大文件操作。
- 取消是正常控制流，不应被记录为未知异常。

## 6. WePChat 特有约束

### 6.1 工作区边界

- 文件、图片和脚本工具只能操作当前会话工作区。
- 工具层不得接收或拼接工作区外的任意路径。
- 路径规范化、越界检测和文件大小限制集中放在工作区基础设施层。
- Android 和 Windows 的真实存储差异只能出现在平台适配层，不能泄漏到工具业务代码。

### 6.2 工具实现

- 模型可见的工具必须拥有稳定名称、简短描述、明确 schema 和结构化结果。
- schema 使用各供应商都容易支持的 JSON Schema 子集，避免无必要的 `oneOf`、递归引用和复杂联合结构。
- 工具执行前统一经过权限策略检查。
- 读取类工具可以并行；同一工作区的写入、编辑和删除必须串行。
- 工具不直接修改 UI；通过统一的 Agent/tool 事件向上层报告进度和结果。
- `preview`、文件导入导出、移动重命名等纯 App 行为不要重复包装成模型工具。

### 6.3 记忆实现

- 全局记忆是 App 私有数据，不是当前会话工作区文件。
- 只能通过 `save_memory`、`list_memory`、`read_memory` 访问。
- 记忆工具维护单一结构化数据源，不在不同目录复制多份记忆。
- 记忆更新遵循明确的 Key 规则，不做不可解释的自动合并。
- 用户可以在设置中查看、编辑和删除记忆。

### 6.4 JavaScript 运行时

- `run_js` 只能通过宿主桥接访问当前工作区。
- 不暴露 Node.js、Shell、进程、环境变量、任意网络或工作区外路径。
- QuickJS 插件只通过 `WepJsRuntime` 抽象使用。
- 超时、取消、输出上限和文件读写上限由宿主统一控制。
- 不在多个模块中分别实现 JS 文件桥接、路径校验或超时逻辑。

## 7. 跨平台结构

- 业务逻辑优先放在纯 Dart 层。
- Android、Windows 的文件选择、路径权限、原生库加载和系统能力放在明确的平台适配接口之后。
- 不在业务代码中散落 `Platform.isAndroid`、`Platform.isWindows` 分支。
- 平台差异无法避免时，集中在一个小模块，并为每个平台实现同一接口。
- 新增依赖前先确认是否同时支持目标平台、许可证、维护状态和包体积。
- 不因为某个平台暂时缺少能力，就在另一个平台复制一套完全不同的业务实现。

## 8. 依赖与复用

- 新增依赖前先检查现有依赖和已有工具是否能解决问题。
- 仅为一个很小的字符串、路径或集合操作引入依赖是不合适的。
- 依赖只能通过基础设施层进入业务，不得让第三方类型渗透整个领域层。
- 原生依赖必须评估 Android ABI、Windows 打包、体积和升级成本。
- 不复制第三方源码到项目中，除非有明确许可证和维护理由。

## 9. 测试与验证

- 修复 bug 时优先增加能够复现问题的测试。
- 工具必须测试：正常结果、参数错误、权限拒绝、越界路径、超时、取消和外部失败。
- 工作区工具必须测试 Android/Windows 无关的领域逻辑，以及平台适配的关键差异。
- JS 运行时必须测试：基础语法、Promise、文件读写、异常、死循环中断和资源限制。
- 不用大量快照测试掩盖缺少行为断言。
- Agent 可以补充或修改测试，但不默认执行复杂测试和完整验证。
- 涉及真实设备、网络、API Key、图片服务、原生库、长时间运行、完整构建或集成环境的测试，默认交给用户执行。
- Agent 只运行成本低、反馈明确且不会消耗外部资源的检查，例如格式化、类型检查或针对性单元测试；是否执行由任务规模决定。
- 不得为了声称“已验证”而运行没有必要的完整测试套件。
- Agent 没有实际运行的测试，必须明确标为“未执行”，不能写成“通过”。

可选的轻量检查：

```bash
dart format --set-exit-if-changed .
flutter analyze
```

`flutter test`、集成测试、真机测试、Windows 打包测试和联网测试，除非用户明确要求，否则不由 Agent 默认执行。

## 10. Agent 工作流程

每次实现任务遵循以下顺序：

1. 明确任务范围、受影响模块和验收条件。
2. 搜索已有实现、调用方和测试。
3. 先选择复用、扩展还是拆分；不要直接复制代码。
4. 进行最小、局部、可解释的修改。
5. 补充或更新相关测试。
6. 根据成本和任务需要执行轻量检查；复杂验证留给用户。
7. 检查 diff，确认没有无关重构、重复实现、静默兜底或文件超长。
8. 汇报实际完成内容、已执行检查、建议用户执行的测试和仍存在的限制。

如果发现当前架构无法满足任务，应先报告冲突和建议，不要用隐式 fallback 或临时重复实现掩盖问题。

## 11. Agent 与用户的协作边界

### 11.1 Agent 的职责

本地 Agent 主要负责代码开发：

- 阅读和理解现有代码。
- 设计并实现用户明确要求的功能。
- 进行必要的局部重构、类型检查和低成本自检。
- 编写或更新测试代码，但不默认承担完整测试流程。
- 在任务结束时列出用户可以直接执行的测试项。

Agent 不默认负责：

- 完整回归测试。
- Android 真机和 Windows 实机验证。
- 真实 API、搜索服务和图片服务验证。
- 长时间运行、压力、性能和稳定性测试。
- 完整构建、打包、签名和发布验证。

### 11.2 节省 Token 和时间

- 不为了证明简单修改而运行完整测试套件。
- 不重复读取已经确认未变化的大文件。
- 不在最终回复中粘贴大段源码，除非用户明确要求。
- 不对与任务无关的代码做审查、格式化或重构。
- 不为了追求理论上的全覆盖而增加大量低价值测试。
- 如果轻量检查已经足够发现明显问题，应停止继续验证。

### 11.3 每项工作的交付格式

任务结束时必须说明：

```text
完成内容：
- 实际修改了什么

已执行检查：
- 运行了哪些命令或测试
- 结果是什么

建议用户测试：
- 用户需要手动验证的功能路径
- 需要 Android/Windows、网络、API 或真实文件的项目

未验证事项：
- Agent 明确没有执行的复杂测试
- 当前已知限制或风险
```

不得把“建议用户测试”写成“测试已通过”，不得隐瞒未执行的验证。

### 11.4 用户反馈优先

- 用户报告问题后，先复现或定位，再决定是否修改。
- 不因为本地静态检查通过，就否定用户的实际运行结果。
- 如果无法在本地复现，说明环境差异和需要用户提供的日志，不编造原因。
- 用户要求“先实现、我来测”时，严格遵循该边界。

## 12. 代码审查清单

提交前检查：

- [ ] 是否复用了已有实现？
- [ ] 是否新增了重复的路径、权限、错误或序列化逻辑？
- [ ] 是否存在无意义的 `try/catch`、空结果或静默降级？
- [ ] 是否把真正的边界校验放在了正确的入口？
- [ ] 是否有文件超过 800 行？
- [ ] 是否把平台分支泄漏到了业务层？
- [ ] 是否正确处理取消、超时和页面销毁？
- [ ] 是否记录了必要错误上下文但避免敏感信息？
- [ ] 是否补充了关键行为测试？
- [ ] 是否有超出当前任务范围的重构？
- [ ] 是否明确区分了 Agent 已执行的检查和建议用户执行的测试？

代码应当让下一位维护者能够快速回答三个问题：

```text
这段逻辑负责什么？
它为什么在这里？
如果失败，调用方会得到什么？
```
